services:
  parts-search:
    build: .
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 3000
    ports:
      - "${HOST_PORT:-3000}:3000"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
