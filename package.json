{
  "name": "parts-search-prototype",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Prototype web interface for searching spare parts through supplier APIs.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node scripts/check-js.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
